name: mimage
description: "Image merging tool"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cross_file: ^0.3.5+2
  file_saver: ^0.3.1
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  image: ^4.8.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  
flutter:

  uses-material-design: true
